# airline-random-forest
Random Forest Classification pipeline utilizing a strict three-way data split, GridSearchCV with PredefinedSplit hyperparameter tuning, and an algorithmic performance comparison matrix against single Decision Trees.
